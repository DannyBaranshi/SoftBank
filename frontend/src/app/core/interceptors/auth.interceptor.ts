// AuthInterceptor was removed by request. Keep this file as a harmless placeholder
// to avoid import errors in older builds while explicit header injection is used
// via service-level `authHeaders()` methods.

// If you want to fully remove this file, delete it after ensuring no imports
// reference it. Currently the app configuration no longer registers the
// interceptor in `app.config.ts`.

export const AuthInterceptorPlaceholder = true;
